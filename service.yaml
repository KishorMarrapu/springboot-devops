apiVersion: v1
kind: Service
metadata:
  name: springboot-service
  namespace: sampledemo
spec:
  type: NodePort
  selector:
    app: springboot-app
  ports:
    - port: 8094
      targetPort: 8094
      nodePort: 30081